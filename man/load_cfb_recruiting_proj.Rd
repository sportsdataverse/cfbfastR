% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_cfb_datasets.R
\name{load_cfb_recruiting_proj}
\alias{load_cfb_recruiting_proj}
\title{\strong{Load college football recruiting projections from the SportsDataverse data repo}}
\usage{
load_cfb_recruiting_proj(
  seasons = most_recent_cfb_season(),
  ...,
  dbConnection = NULL,
  tablename = NULL
)
}
\arguments{
\item{seasons}{A vector of 4-digit years associated with given college football seasons. Published coverage runs 2016 through the most recent season. Pass \code{seasons = TRUE} for every published season. (Min: 2016)}

\item{...}{Additional arguments passed to an underlying function that
writes the season data into a database.}

\item{dbConnection}{A \code{DBIConnection} object, as returned by \code{\link[DBI:dbConnect]{DBI::dbConnect()}}}

\item{tablename}{The name of the data table within the database}
}
\value{
Returns a \code{cfbfastR_data} tibble.\tabular{lll}{
   col_name \tab types \tab description \cr
   season \tab integer \tab  \cr
   team_id \tab integer \tab  \cr
   pred_wins \tab double \tab Ridge projection of the team's season win total, fit strictly on prior seasons from talent composite, blue-chip ratio, offensive and defensive returning production, and prior wins. \cr
   pred_margin \tab double \tab Ridge projection of the team's average per-game scoring margin, from the same preseason-known feature set as pred_wins. \cr
   pred_net_epa \tab double \tab Reserved slot for a projected adjusted net EPA; it ships all-null because the adjusted-EPA training target is not currently loadable. \cr
}
}
\description{
Loads recruiting-based team projections -- one row per team-season with
talent-derived projection inputs. Published to the
\code{cfb_recruiting_proj} release tag on the sportsdataverse-data repo.
}
\examples{
\donttest{
  try(load_cfb_recruiting_proj(2016))
}
}
\author{
Saiem Gilani
}
