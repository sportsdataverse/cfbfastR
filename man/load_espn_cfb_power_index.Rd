% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_espn_cfb.R
\name{load_espn_cfb_power_index}
\alias{load_espn_cfb_power_index}
\title{\strong{Load ESPN college football FPI power index from the SportsDataverse data repo}}
\usage{
load_espn_cfb_power_index(
  seasons = most_recent_cfb_season(),
  ...,
  dbConnection = NULL,
  tablename = NULL
)
}
\arguments{
\item{seasons}{A vector of 4-digit years associated with given college football seasons. Published coverage runs 2015 through the most recent season. Pass \code{seasons = TRUE} for every published season. (Min: 2015)}

\item{...}{Additional arguments passed to an underlying function that
writes the season data into a database.}

\item{dbConnection}{A \code{DBIConnection} object, as returned by \code{\link[DBI:dbConnect]{DBI::dbConnect()}}}

\item{tablename}{The name of the data table within the database}
}
\value{
Returns a \code{cfbfastR_data} tibble.\tabular{lll}{
   col_name \tab types \tab description \cr
   season \tab integer \tab  \cr
   game_id \tab integer \tab  \cr
   team_id \tab integer \tab  \cr
   teampredptdiff \tab double \tab Expected margin of victory for the FPI favorite. \cr
   gameprojection \tab double \tab Team's predicted win percentage in this game at time of given BPI run. \cr
   matchupquality \tab double \tab A measure of projected competitiveness and excitement in the game, using a 0 to 100 scale, with 100 as the most exciting. \cr
   teamadjgamescore \tab double \tab A measure of how well a team performed compared to their expected performance and the expected performance of a typical top 25 team. \cr
}
}
\description{
Loads season-level ESPN FPI power-index snapshots -- one row per team
with FPI rating, projected wins, and efficiency components. Published
to the
\code{espn_cfb_power_index} release tag on the sportsdataverse-data repo.
}
\examples{
\donttest{
  try(load_espn_cfb_power_index(2015))
}
}
\author{
Saiem Gilani
}
