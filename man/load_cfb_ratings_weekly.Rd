% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_cfb_datasets.R
\name{load_cfb_ratings_weekly}
\alias{load_cfb_ratings_weekly}
\title{\strong{Load college football weekly power ratings from the SportsDataverse data repo}}
\usage{
load_cfb_ratings_weekly(
  seasons = most_recent_cfb_season(),
  ...,
  dbConnection = NULL,
  tablename = NULL
)
}
\arguments{
\item{seasons}{A vector of 4-digit years associated with given college football seasons. Published coverage runs 2004 through the most recent season. Pass \code{seasons = TRUE} for every published season. (Min: 2004)}

\item{...}{Additional arguments passed to an underlying function that
writes the season data into a database.}

\item{dbConnection}{A \code{DBIConnection} object, as returned by \code{\link[DBI:dbConnect]{DBI::dbConnect()}}}

\item{tablename}{The name of the data table within the database}
}
\value{
Returns a \code{cfbfastR_data} tibble.\tabular{lll}{
   col_name \tab types \tab description \cr
   season \tab integer \tab  \cr
   team_id \tab integer \tab  \cr
   adj_off_epa \tab double \tab Opponent-adjusted offensive EPA per play as of the snapshot week: raw per-game EPA on pass and rush plays net of each opponent's ridge-fitted defensive strength. \cr
   adj_def_epa \tab double \tab Opponent-adjusted EPA per play allowed as of the snapshot week, netted the same way as the offensive rating, so lower is better. \cr
   adj_st_epa \tab double \tab Special-teams composite in EPA units as of the snapshot week, summing the league-centered per-play EPA of the field goal, punt, and kick-return units. \cr
   adj_net \tab double \tab adj_off_epa minus adj_def_epa at the snapshot week, the team's overall efficiency rating in EPA per play with special teams excluded. \cr
   fei_off \tab double \tab Drive-level offensive rating at the snapshot week, from a ridge fit on per-drive EPA. \cr
   fei_def \tab double \tab Drive-level defensive rating at the snapshot week, from the same per-drive ridge fit and on the same scale as fei_off. \cr
   fei_net \tab double \tab fei_off minus fei_def at the snapshot week, the team's overall drive-efficiency rating. \cr
   games \tab integer \tab  \cr
   off_pace \tab double \tab Scrimmage plays per game through the snapshot week, the tempo input consumed by the totals model. \cr
   off_rank \tab integer \tab Dense rank of adj_off_epa in descending order within the snapshot week, so rank 1 is the most efficient offense at that point. \cr
   def_rank \tab integer \tab Dense rank of adj_def_epa in ascending order within the snapshot week, so rank 1 is the stingiest defense at that point. \cr
   net_rank \tab integer \tab Dense rank of adj_net in descending order within the snapshot week, so rank 1 is the strongest overall team at that point. \cr
   net_z \tab double \tab adj_net restated as a z-score against the mean and standard deviation of adj_net across the teams rated in that snapshot week. \cr
   through_week \tab integer \tab Regular-season week the snapshot runs through; the ratings were refit using only games kicking off on or before that week's final kickoff date. \cr
}
}
\description{
Loads weekly team power ratings -- one row per team-week, the as-of-
week snapshots behind the season-end ratings. Published to the
\code{cfb_ratings_weekly} release tag on the sportsdataverse-data repo.
}
\examples{
\donttest{
  try(load_cfb_ratings_weekly(2004))
}
}
\author{
Saiem Gilani
}
