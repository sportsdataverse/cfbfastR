% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/yahoo_cfb.R
\name{yahoo_cfb_scoreboard}
\alias{yahoo_cfb_scoreboard}
\title{\strong{Get Yahoo Sports college football scoreboard}}
\usage{
yahoo_cfb_scoreboard(season, week = 1, count = 500)
}
\arguments{
\item{season}{(integer, required): Season year.}

\item{week}{(integer): Schedule week. Defaults to \code{1}.}

\item{count}{(integer): Max games. Defaults to \code{500}.}
}
\value{
A \code{cfbfastR}-tagged tibble with one row per game; columns are the
Yahoo game fields (\code{gameid}, \code{home_team_id}, \code{away_team_id},
\code{total_home_points}, \code{total_away_points}, \code{status_type}, ...) plus \code{season}
and \code{week}. Column set varies with game state.
}
\description{
Flattens the editorial \code{scoreboard} games map into one tibble (one row per
game). The full payload also embeds teams/leagues/odds maps.
}
\examples{
\donttest{
  try(yahoo_cfb_scoreboard(season = 2024, week = 1))
}
}
\keyword{Data}
\keyword{Schedule}
