% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_espn_cfb.R
\name{load_espn_cfb_model_pbp}
\alias{load_espn_cfb_model_pbp}
\title{\strong{Load college football model-enriched play-by-play from the SportsDataverse data repo}}
\usage{
load_espn_cfb_model_pbp(
  seasons = most_recent_cfb_season(),
  ...,
  dbConnection = NULL,
  tablename = NULL
)
}
\arguments{
\item{seasons}{A vector of 4-digit years associated with given college football seasons. Published coverage runs 2004 through the most recent season. Pass \code{seasons = TRUE} for every published season. (Min: 2004)}

\item{...}{Additional arguments passed to an underlying function that
writes the season data into a database.}

\item{dbConnection}{A \code{DBIConnection} object, as returned by \code{\link[DBI:dbConnect]{DBI::dbConnect()}}}

\item{tablename}{The name of the data table within the database}
}
\value{
Returns a \code{cfbfastR_data} tibble.\tabular{lll}{
   col_name \tab types \tab description \cr
   game_id \tab integer \tab  \cr
   id \tab character \tab  \cr
   sequenceNumber \tab character \tab  \cr
   game_play_number \tab integer \tab  \cr
   drive.id \tab character \tab ESPN's drive identifier, formed as the game id followed by the drive's sequence number within that game. \cr
   season \tab integer \tab  \cr
   week \tab integer \tab  \cr
   period \tab integer \tab  \cr
   pos_team \tab integer \tab  \cr
   def_pos_team \tab integer \tab  \cr
   start.pos_team.name \tab character \tab School name of the team with possession at the snap, taken from ESPN's team location field so it carries no mascot. \cr
   homeTeamId \tab integer \tab ESPN team id of the home team, read off the game header and stamped on every play. \cr
   awayTeamId \tab integer \tab ESPN team id of the away team, read off the game header and stamped on every play. \cr
   homeTeamName \tab character \tab Home team's school name from ESPN's team location field, without the mascot. \cr
   awayTeamName \tab character \tab Away team's school name from ESPN's team location field, without the mascot. \cr
   type.text \tab character \tab ESPN's play-type label, for example Rush, Pass Reception, Sack, Punt, Penalty, or Timeout. \cr
   text \tab character \tab  \cr
   start.down \tab integer \tab Down at the snap as ESPN reports it; 0 marks the small share of rows ESPN leaves without a down, overwhelmingly timeouts and penalty administrations. \cr
   start.distance \tab integer \tab Yards the offense needs for a first down at the snap, carried through from ESPN without correction. \cr
   start.yardsToEndzone \tab integer \tab Distance in yards from the offense's spot at the snap to the opponent's end zone, ranging 0 to 100. \cr
   pos_score_diff_start \tab integer \tab  \cr
   start.TimeSecsRem \tab integer \tab Seconds remaining in the half at the snap, so it tops out at 1800 rather than counting down from a full game. \cr
   start.is_home \tab logical \tab True when the team holding possession at the snap is the home team. \cr
   passing_down \tab logical \tab True on second and eight or longer, third and five or longer, or fourth and five or longer, the standard obvious-passing-situation flag. \cr
   pass \tab logical \tab  \cr
   rush \tab logical \tab  \cr
   completion \tab logical \tab  \cr
   scoring_play \tab logical \tab  \cr
   statYardage \tab integer \tab Yards gained on the play as ESPN reports it, negative on plays that lost yardage. \cr
   passer_player_name \tab character \tab Display name of the passer -- the FIRST participant in that role on the play. \cr
   ep_before \tab double \tab  \cr
   ep_after \tab double \tab  \cr
   epa \tab double \tab  \cr
   wp_before \tab double \tab  \cr
   wp_after \tab double \tab  \cr
   wpa \tab double \tab  \cr
   completion_prob \tab double \tab Modelled probability the pass is completed. \cr
   cpoe \tab double \tab  \cr
   model_pbp_version \tab character \tab Version of the model-scored play-by-play build. \cr
   cp_model_version \tab character \tab Version of the completion-probability model that scored the play. \cr
   ep_model_version \tab character \tab Version of the expected-points model that scored the play. \cr
   wp_model_version \tab character \tab Version of the win-probability model that scored the play. \cr
   scored_date \tab character \tab Date on which the play was scored by the models. \cr
}
}
\description{
Loads season-level model-enriched play-by-play -- the modeling-grade
pbp frame with the engineered features the cfbfastR EP/WP model suite
trains on. Published to the
\code{espn_cfb_model_pbp} release tag on the sportsdataverse-data repo.
}
\examples{
\donttest{
  try(load_espn_cfb_model_pbp(2004))
}
}
\author{
Saiem Gilani
}
