% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_espn_cfb.R
\name{load_espn_cfb_play_participants}
\alias{load_espn_cfb_play_participants}
\title{\strong{Load ESPN college football play participants from the SportsDataverse data repo}}
\usage{
load_espn_cfb_play_participants(
  seasons = most_recent_cfb_season(),
  ...,
  dbConnection = NULL,
  tablename = NULL
)
}
\arguments{
\item{seasons}{A vector of 4-digit years associated with given college football seasons. Published coverage runs 2014 through the most recent season. Pass \code{seasons = TRUE} for every published season. (Min: 2014)}

\item{...}{Additional arguments passed to an underlying function that
writes the season data into a database.}

\item{dbConnection}{A \code{DBIConnection} object, as returned by \code{\link[DBI:dbConnect]{DBI::dbConnect()}}}

\item{tablename}{The name of the data table within the database}
}
\value{
Returns a \code{cfbfastR_data} tibble.\tabular{lll}{
   col_name \tab types \tab description \cr
   game_id \tab integer \tab  \cr
   play_id \tab integer \tab  \cr
   kicker_player_name \tab character \tab Display name of the kicker -- the FIRST participant in that role on the play. \cr
   tackler_player_name \tab character \tab Display name of a defender credited with the tackle -- the FIRST participant in that role on the play. \cr
   returner_player_name \tab character \tab Display name of the player returning the kick or punt -- the FIRST participant in that role on the play. \cr
   rusher_player_name \tab character \tab Display name of the ball carrier on a rush -- the FIRST participant in that role on the play. \cr
   passer_player_name \tab character \tab Display name of the passer -- the FIRST participant in that role on the play. \cr
   receiver_player_name \tab character \tab Display name of the targeted receiver -- the FIRST participant in that role on the play. \cr
   punter_player_name \tab character \tab Display name of the punter -- the FIRST participant in that role on the play. \cr
   assisted_by_player_name \tab character \tab Display name of a defender credited with an assisted tackle -- the FIRST participant in that role on the play. \cr
   penalized_player_name \tab character \tab Display name of the penalized player -- the FIRST participant in that role on the play. \cr
   scorer_player_name \tab character \tab Display name of the player credited with the score -- the FIRST participant in that role on the play. \cr
   pat_scorer_player_name \tab character \tab Display name of the player credited with the point-after score -- the FIRST participant in that role on the play. \cr
   sacked_by_player_name \tab character \tab Display name of a defender credited with the sack -- the FIRST participant in that role on the play. \cr
   kicker_player_id \tab character \tab ESPN athlete id of the kicker -- the FIRST participant in that role on the play. \cr
   tackler_player_id \tab character \tab ESPN athlete id of a defender credited with the tackle -- the FIRST participant in that role on the play. \cr
   returner_player_id \tab character \tab ESPN athlete id of the player returning the kick or punt -- the FIRST participant in that role on the play. \cr
   rusher_player_id \tab character \tab ESPN athlete id of the ball carrier on a rush -- the FIRST participant in that role on the play. \cr
   passer_player_id \tab character \tab ESPN athlete id of the passer -- the FIRST participant in that role on the play. \cr
   receiver_player_id \tab character \tab ESPN athlete id of the targeted receiver -- the FIRST participant in that role on the play. \cr
   punter_player_id \tab character \tab ESPN athlete id of the punter -- the FIRST participant in that role on the play. \cr
   assisted_by_player_id \tab character \tab ESPN athlete id of a defender credited with an assisted tackle -- the FIRST participant in that role on the play. \cr
   penalized_player_id \tab character \tab ESPN athlete id of the penalized player -- the FIRST participant in that role on the play. \cr
   scorer_player_id \tab character \tab ESPN athlete id of the player credited with the score -- the FIRST participant in that role on the play. \cr
   pat_scorer_player_id \tab character \tab ESPN athlete id of the player credited with the point-after score -- the FIRST participant in that role on the play. \cr
   sacked_by_player_id \tab character \tab ESPN athlete id of a defender credited with the sack -- the FIRST participant in that role on the play. \cr
   kicker_player_names \tab character \tab List of the display names of EVERY participant credited as the kicker on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   tackler_player_names \tab character \tab List of the display names of EVERY participant credited as a defender credited with the tackle on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   returner_player_names \tab character \tab List of the display names of EVERY participant credited as the player returning the kick or punt on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   rusher_player_names \tab character \tab List of the display names of EVERY participant credited as the ball carrier on a rush on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   passer_player_names \tab character \tab List of the display names of EVERY participant credited as the passer on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   receiver_player_names \tab character \tab List of the display names of EVERY participant credited as the targeted receiver on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   punter_player_names \tab character \tab List of the display names of EVERY participant credited as the punter on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   assisted_by_player_names \tab character \tab List of the display names of EVERY participant credited as a defender credited with an assisted tackle on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   penalized_player_names \tab character \tab List of the display names of EVERY participant credited as the penalized player on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   scorer_player_names \tab character \tab List of the display names of EVERY participant credited as the player credited with the score on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   pat_scorer_player_names \tab character \tab List of the display names of EVERY participant credited as the player credited with the point-after score on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   sacked_by_player_names \tab character \tab List of the display names of EVERY participant credited as a defender credited with the sack on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   kicker_player_ids \tab character \tab List of the athlete ids of EVERY participant credited as the kicker on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   tackler_player_ids \tab character \tab List of the athlete ids of EVERY participant credited as a defender credited with the tackle on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   returner_player_ids \tab character \tab List of the athlete ids of EVERY participant credited as the player returning the kick or punt on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   rusher_player_ids \tab character \tab List of the athlete ids of EVERY participant credited as the ball carrier on a rush on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   passer_player_ids \tab character \tab List of the athlete ids of EVERY participant credited as the passer on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   receiver_player_ids \tab character \tab List of the athlete ids of EVERY participant credited as the targeted receiver on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   punter_player_ids \tab character \tab List of the athlete ids of EVERY participant credited as the punter on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   assisted_by_player_ids \tab character \tab List of the athlete ids of EVERY participant credited as a defender credited with an assisted tackle on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   penalized_player_ids \tab character \tab List of the athlete ids of EVERY participant credited as the penalized player on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   scorer_player_ids \tab character \tab List of the athlete ids of EVERY participant credited as the player credited with the score on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   pat_scorer_player_ids \tab character \tab List of the athlete ids of EVERY participant credited as the player credited with the point-after score on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   sacked_by_player_ids \tab character \tab List of the athlete ids of EVERY participant credited as a defender credited with the sack on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   season \tab integer \tab  \cr
   week \tab integer \tab  \cr
   pass_defender_player_name \tab character \tab Display name of the defender credited with defending the pass -- the FIRST participant in that role on the play. \cr
   pass_defender_player_id \tab character \tab ESPN athlete id of the defender credited with defending the pass -- the FIRST participant in that role on the play. \cr
   pass_defender_player_names \tab character \tab List of the display names of EVERY participant credited as the defender credited with defending the pass on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   pass_defender_player_ids \tab character \tab List of the athlete ids of EVERY participant credited as the defender credited with defending the pass on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   recoverer_player_name \tab character \tab Display name of the player who recovered the fumble -- the FIRST participant in that role on the play. \cr
   fumbler_player_name \tab character \tab  \cr
   recoverer_player_id \tab character \tab ESPN athlete id of the player who recovered the fumble -- the FIRST participant in that role on the play. \cr
   fumbler_player_id \tab character \tab  \cr
   recoverer_player_names \tab character \tab List of the display names of EVERY participant credited as the player who recovered the fumble on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   fumbler_player_names \tab character \tab  \cr
   recoverer_player_ids \tab character \tab List of the athlete ids of EVERY participant credited as the player who recovered the fumble on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   fumbler_player_ids \tab character \tab  \cr
   forced_by_player_name \tab character \tab Display name of the defender who forced the fumble -- the FIRST participant in that role on the play. \cr
   forced_by_player_id \tab character \tab ESPN athlete id of the defender who forced the fumble -- the FIRST participant in that role on the play. \cr
   forced_by_player_names \tab character \tab List of the display names of EVERY participant credited as the defender who forced the fumble on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   forced_by_player_ids \tab character \tab List of the athlete ids of EVERY participant credited as the defender who forced the fumble on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   pat_passer_player_name \tab character \tab Display name of the passer on the point-after attempt -- the FIRST participant in that role on the play. \cr
   pat_passer_player_id \tab character \tab ESPN athlete id of the passer on the point-after attempt -- the FIRST participant in that role on the play. \cr
   pat_passer_player_names \tab character \tab List of the display names of EVERY participant credited as the passer on the point-after attempt on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
   pat_passer_player_ids \tab character \tab List of the athlete ids of EVERY participant credited as the passer on the point-after attempt on the play, so multi-entry roles such as split sacks or gang tackles are not collapsed to one. \cr
}
}
\description{
Load espn_cfb_play_participants (sportsdataverse-data release). Published
to the \code{espn_cfb_play_participants} release tag on the sportsdataverse-
data repo.
}
\examples{
\donttest{
  try(load_espn_cfb_play_participants(2014))
}
}
\author{
Saiem Gilani
}
