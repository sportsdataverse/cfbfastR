% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pbp_qbr.R
\name{create_qbr}
\alias{create_qbr}
\title{Calculate QBR for each quarterback in a modeled play-by-play frame}
\usage{
create_qbr(play_df, qbr_model = NULL, season = NULL)
}
\arguments{
\item{play_df}{A modeled play-by-play data frame, as returned by
\code{\link[=cfbd_pbp_data]{cfbd_pbp_data()}} or \code{\link[=espn_cfb_pbp]{espn_cfb_pbp()}}. Must carry \code{EPA}, \code{home_wp_before},
the \code{pass} / \code{rush} / \code{sack_vec} / \code{fumble_vec} / \code{penalty_flag} play flags,
\code{passer_player_name} / \code{rusher_player_name} and \code{pos_team}. \code{spread} and
\code{home} are additionally needed for the \code{spread} feature.}

\item{qbr_model}{Optional pre-loaded \code{xgb.Booster}. Defaults to the bundled
\code{qbr_model.ubj} from the shared \code{cfb_model_artifacts} release.}

\item{season}{Optional season, used for the one-hot rule era when the frame
carries no \code{season} column.}
}
\value{
A data frame, one row per quarterback per game, with:\tabular{ll}{
   col_name \tab types \cr
   game_id \tab character \cr
   pos_team \tab character \cr
   athlete_name \tab character \cr
   plays \tab integer \cr
   qbr_epa \tab double \cr
   sack_epa \tab double \cr
   pass_epa \tab double \cr
   rush_epa \tab double \cr
   pen_epa \tab double \cr
   spread \tab double \cr
   era0 \tab double \cr
   era1 \tab double \cr
   era2 \tab double \cr
   era3 \tab double \cr
   exp_qbr \tab double \cr
}


A component with no qualifying plays is \code{NA} rather than 0 -- that is the
missing-value branch the model was trained against. \code{exp_qbr} is \code{NA} when
the bundled model or \code{xgboost} is unavailable; probabilities and ratings are
never fabricated. A frame with no identifiable quarterbacks returns a
zero-row frame carrying this schema.
}
\description{
Aggregates a modeled play-by-play frame into the per-quarterback,
per-game weighted EPA components the bundled \code{qbr_model} takes, and scores it.

The five EPA components are leverage-weighted means over the plays a
quarterback was involved in: every play (\code{qbr_epa}), non-fumble sacks
(\code{sack_epa}), passes (\code{pass_epa}), rushes (\code{rush_epa}) and penalties
(\code{pen_epa}). The weight discounts garbage time: a play taken with the game
already decided (\code{home_wp_before} below 0.1 or above 0.9) counts 0.6, one
taken while it is nearly decided (0.1 to 0.2, or 0.8 to 0.9) counts 0.9, and
everything in the competitive middle counts fully. The EPA feeding them is
clipped at -5, with a flat -3.5 charged for a fumble.

Only players who actually threw a pass are included; a running back's carries
are aggregated into the quarterback's line only when that player also attempted
a pass in the game.
}
\examples{
\donttest{
  try({
    pbp <- cfbfastR::cfbd_pbp_data(2021, week = 6, team = "Texas", epa_wpa = TRUE)
    create_qbr(pbp)
  })
}

}
