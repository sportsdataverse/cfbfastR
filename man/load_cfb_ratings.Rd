% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_cfb_datasets.R
\name{load_cfb_ratings}
\alias{load_cfb_ratings}
\title{\strong{Load college football season power ratings from the SportsDataverse data repo}}
\usage{
load_cfb_ratings(
  seasons = most_recent_cfb_season(),
  ...,
  dbConnection = NULL,
  tablename = NULL
)
}
\arguments{
\item{seasons}{A vector of 4-digit years associated with given college football seasons. Published coverage runs 2004 through the most recent season. Pass \code{seasons = TRUE} for every published season. (Min: 2004)}

\item{...}{Additional arguments passed to an underlying function that
writes the season data into a database.}

\item{dbConnection}{A \code{DBIConnection} object, as returned by \code{\link[DBI:dbConnect]{DBI::dbConnect()}}}

\item{tablename}{The name of the data table within the database}
}
\value{
Returns a \code{cfbfastR_data} tibble.\tabular{lll}{
   col_name \tab types \tab description \cr
   season \tab integer \tab  \cr
   team_id \tab integer \tab  \cr
   adj_off_epa \tab double \tab Opponent-adjusted offensive EPA per play: the team's raw per-game EPA on pass and rush plays net of each opponent's ridge-fitted defensive strength, averaged over its games. \cr
   adj_def_epa \tab double \tab Opponent-adjusted EPA per play allowed, netted the same way as the offensive rating, so lower is better because it measures EPA surrendered. \cr
   adj_st_epa \tab double \tab Special-teams composite in EPA units: per-play mean EPA on field goals, punts, and kick returns, each centered on that unit's league mean and summed across the three units. \cr
   adj_net \tab double \tab adj_off_epa minus adj_def_epa, the team's overall efficiency rating in EPA per play; special teams is deliberately excluded. \cr
   fei_off \tab double \tab Drive-level offensive rating from a ridge fit on per-drive EPA, the Fremeau-style drive-efficiency counterpart to adj_off_epa. \cr
   fei_def \tab double \tab Drive-level defensive rating from the same per-drive ridge fit, on the same scale as fei_off. \cr
   fei_net \tab double \tab fei_off minus fei_def, the team's overall drive-efficiency rating, with the ridge's dropped reference team pinned at zero. \cr
   games \tab integer \tab  \cr
   off_pace \tab double \tab Tempo measure: scrimmage plays (pass plus rush) per game, centering near 65 and used as the pace input to the totals model. \cr
   off_rank \tab integer \tab Dense rank of adj_off_epa in descending order, so rank 1 is the season's most efficient offense. \cr
   def_rank \tab integer \tab Dense rank of adj_def_epa in ascending order, so rank 1 is the season's stingiest defense. \cr
   net_rank \tab integer \tab Dense rank of adj_net in descending order, so rank 1 is the season's strongest overall team. \cr
   net_z \tab double \tab adj_net restated as a z-score against the mean and standard deviation of adj_net across the rated teams that season. \cr
}
}
\description{
Load cfb_ratings (sportsdataverse-data release). Published to the
\code{cfb_ratings} release tag on the sportsdataverse-data repo.
}
\examples{
\donttest{
  try(load_cfb_ratings(2004))
}
}
\author{
Saiem Gilani
}
