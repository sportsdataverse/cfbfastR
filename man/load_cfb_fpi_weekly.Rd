% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_cfb_datasets.R
\name{load_cfb_fpi_weekly}
\alias{load_cfb_fpi_weekly}
\title{\strong{Load college football weekly ESPN FPI ratings from the SportsDataverse data repo}}
\usage{
load_cfb_fpi_weekly(
  seasons = most_recent_cfb_season(),
  ...,
  dbConnection = NULL,
  tablename = NULL
)
}
\arguments{
\item{seasons}{A vector of 4-digit years associated with given college football seasons. Published coverage runs 2005 through the most recent season. Pass \code{seasons = TRUE} for every published season. (Min: 2005)}

\item{...}{Additional arguments passed to an underlying function that
writes the season data into a database.}

\item{dbConnection}{A \code{DBIConnection} object, as returned by \code{\link[DBI:dbConnect]{DBI::dbConnect()}}}

\item{tablename}{The name of the data table within the database}
}
\value{
Returns a \code{cfbfastR_data} tibble.\tabular{lll}{
   col_name \tab types \tab description \cr
   season \tab integer \tab  \cr
   season_type \tab integer \tab  \cr
   week \tab integer \tab  \cr
   team_id \tab integer \tab  \cr
   last_updated \tab character \tab  \cr
   run_date_time_key \tab integer \tab ESPN's run key for the snapshot, as an integer timestamp (e.g. 20241021040000). This is the AS-OF date the snapshot represents, which is not the same as last_updated (when ESPN computed it); the gap between the two is what snapshot_is_contemporaneous flags. \cr
   snapshot_out_of_sequence \tab logical \tab True when this snapshot was computed AFTER one belonging to a later week of the same season type -- so it cannot be read as an as-of-that-week rating. Almost always the week-1 slot, which ESPN overwrites with a late-season computation (2024 week 1 is stamped 2024-12-15). Filter these out for any point-in-time or backtest use. \cr
   fpi \tab double \tab Football Power Index that measures team's true strength on net points scale; expected point margin vs average opponent on neutral field. \cr
   fpirank \tab double \tab ESPN's FPI rank field. Agrees with rank on 99.4\% of rows; on the ~0.6\% where they differ it is stale -- it never matches the rank implied by the published fpi, while rank always does. Prefer rank. \cr
   projectedw \tab double \tab Projected overall W-L, accounting for results to date and FPI-based projections for remaining scheduled games (and potential conference championship games). May not sum to a whole number because of differing number of games played in each simulation. \cr
   projectedl \tab double \tab Projected overall Losses, accounting for results to date and FPI-based projections for remaining scheduled games (including potential conference championship games). May not sum to a whole number because of differing number of games played in each simulation. \cr
   projectedt \tab character \tab Projected ties. Always null -- college football abolished ties in 1996, and ESPN emits the key beside projectedw/projectedl without ever populating it. Retained so the column set matches the upstream payload. \cr
   projectedwpctrank \tab double \tab Rank among FBS teams by projected win percentage. ESPN publishes the rank without the underlying percentage; derive it from projectedw and projectedl. \cr
   probwinout \tab double \tab Percent of season simulations in which team won all remaining scheduled games as well as conference championship game (if applicable). \cr
   probwinconf \tab double \tab Percent of season simulations in which team won its conference, incorporating chance of getting to and winning conference championship game (if applicable). Accounts for shared conference titles in conferences that allow them. \cr
   sosremainingrank \tab double \tab Rank among all FBS teams of remaining schedule strength, from perspective of an average FBS team. \cr
   accomplishment \tab double \tab Reflects chance that an average Top 25 team would have team's record or better, given the schedule. On a 0 to 100 scale, where 100 is best. \cr
   accomplishmentrank \tab double \tab Strength of Record rank. Reflects chance that an average Top 25 team would have team's record or better, given the schedule. \cr
   adjwins \tab double \tab Team's Wins adjusted for chance an average FBS team would have team's record or better, given the schedule. \cr
   adjlosses \tab double \tab Team's Losses adjusted for chance an average FBS team would have team's record or better, given the schedule. \cr
   adjwinpctrank \tab double \tab Rank among FBS teams by adjusted win percentage. ESPN publishes the rank without the underlying percentage; derive it from adjwins and adjlosses. 0 is an unranked placeholder, not a rank -- it appears where the underlying value is null. \cr
   gamecontrol \tab double \tab Reflects chance that an average Top 25 team would control games from start to end the way this team did, given the schedule. On a 0 to 100 scale, where 100 is best. \cr
   gamecontrolrank \tab double \tab Game Control rank. Reflects chance that an average Top 25 team would control games from start to end the way this team did, given the schedule. \cr
   adjavgingamewp \tab double \tab Team's average in-game win probability adjusted for chance that an average FBS team would control games from start to end the way this team did, given the schedule. \cr
   adjavgingamewprank \tab double \tab Rank among FBS teams by adjavgingamewp (average in-game win probability adjusted for opponent). Null for most pre-2019 snapshots. 0 is an unranked placeholder, not a rank. \cr
   avgingamewp \tab double \tab Team's average in-game win probability across all plays of all games played, not adjusted for site or opponent. \cr
   avgingamewprank \tab double \tab Team's average in-game win probability rank adjusted for chance that an average FBS team would control games from start to end the way this team did, given the schedule. \cr
   avgsosrank \tab double \tab Rank among all FBS teams of games already played schedule strength, from perspective of an average Top 25 team. \cr
   topsosrank \tab double \tab Rank among all FBS teams of games already played schedule strength, from perspective of an top FBS team. \cr
   epaoffense \tab double \tab Offensive component of FPI. Offensive contribution to expected point margin vs average opponent on neutral field. \cr
   epadefense \tab double \tab Defensive component of FPI. Defensive contribution to expected point margin vs average opponent on neutral field. \cr
   epaspecialteams \tab double \tab Special teams component of FPI. Special teams contribution to expected point margin vs average opponent on neutral field. \cr
   probwindiv \tab double \tab Percent of season simulations in which team won its conference division, for those conferences that have divisions. \cr
   probmakeplayoffs \tab double \tab Chance to make the CFB Playoff, according to the Playoff Predictor. \cr
   probmaketitlegame \tab double \tab Chance to make the CFB Playoff National Championship game, according to the Playoff Predictor. \cr
   numwins \tab double \tab Actual wins to date at the time of the snapshot. Distinct from projectedw (full-season projection) and adjwins (opponent-adjusted). \cr
   numlosses \tab double \tab Actual losses to date at the time of the snapshot. Distinct from projectedl (full-season projection) and adjlosses (opponent-adjusted). \cr
   numties \tab double \tab Actual ties to date. Never nonzero -- college football abolished ties in 1996; the column is null or 0 in every published row. \cr
   probwintitle \tab double \tab Chance to win the CFB Playoff National Championship, according to the Playoff Predictor. \cr
   rankchange7days \tab double \tab FPI Rank change from previous week. \cr
   prob6wins \tab double \tab Percent of season simulations in which a team won at least 6 games (typically bowl-eligible). \cr
   rank \tab double \tab FPI rank among FBS teams for this snapshot (1 = best). Prefer this over fpirank: the two agree on 99.4\% of rows, and on the ~0.6\% where they differ, rank is always the one consistent with the published fpi value. \cr
   offefficiency \tab double \tab Offensive efficiency on 0-100 scale; based on offense's contribution to scoring margin on per-play basis, adjusted for strength of opposing defenses faced. \cr
   offefficiencyrank \tab double \tab Team's offensive efficiency rank among all FBS teams. \cr
   defefficiency \tab double \tab Defensive efficiency on 0-100 scale; based on defense's contribution to scoring margin on per-play basis, adjusted for strength of opposing offenses faced. \cr
   defefficiencyrank \tab double \tab Team's defensive efficiency rank among all FBS teams. \cr
   stefficiency \tab double \tab Special teams efficiency on 0-100 scale; based on special teams' contribution to scoring margin on per-play basis, adjusted for strength of opposing special teams faced. \cr
   stefficiencyrank \tab double \tab Team's special teams efficiency rank among all FBS teams. \cr
   totefficiency \tab double \tab Net efficiency on 0-100 scale; incorporates offense, defense and special teams efficiencies into a single schedule-adjusted measure of per-play efficiency. \cr
   totefficiencyrank \tab double \tab Team's overall efficiency rank among all FBS teams. \cr
   snapshot_is_contemporaneous \tab logical \tab True when the snapshot was computed inside its own season's window (August of the season year through February of the next), i.e. it is a live weekly run rather than a retrospective backfill. False for every row before 2015, which ESPN computed in one pass afterwards. A retrospective row is a reconstruction, not an as-of-week rating. \cr
}
}
\description{
Loads weekly ESPN FPI snapshots -- one row per team-week with FPI and
its components as published that week. Published to the
\code{cfb_fpi_weekly} release tag on the sportsdataverse-data repo.
}
\examples{
\donttest{
  try(load_cfb_fpi_weekly(2005))
}
}
\author{
Saiem Gilani
}
