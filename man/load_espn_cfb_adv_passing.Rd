% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_espn_cfb.R
\name{load_espn_cfb_adv_passing}
\alias{load_espn_cfb_adv_passing}
\title{\strong{Load college football advanced passing from the SportsDataverse data repo}}
\usage{
load_espn_cfb_adv_passing(
  seasons = most_recent_cfb_season(),
  ...,
  dbConnection = NULL,
  tablename = NULL
)
}
\arguments{
\item{seasons}{A vector of 4-digit years associated with given college football seasons. Published coverage runs 2004 through the most recent season. Pass \code{seasons = TRUE} for every published season. (Min: 2004)}

\item{...}{Additional arguments passed to an underlying function that
writes the season data into a database.}

\item{dbConnection}{A \code{DBIConnection} object, as returned by \code{\link[DBI:dbConnect]{DBI::dbConnect()}}}

\item{tablename}{The name of the data table within the database}
}
\value{
Returns a \code{cfbfastR_data} tibble.\tabular{lll}{
   col_name \tab types \tab description \cr
   pos_team_id \tab integer \tab ESPN team id of the team on offense. Present for every season 2004+. \cr
   pos_team \tab character \tab  \cr
   passer_player_name \tab character \tab Display name of the passer -- the FIRST participant in that role on the play. \cr
   Comp \tab integer \tab Completed passes recorded in the advanced box score. \cr
   Att \tab integer \tab Pass attempts recorded in the advanced box score. \cr
   xComp \tab double \tab Expected completions, summed from the per-play completion model. \cr
   Yds \tab double \tab Passing yards from the advanced box score. \cr
   Pass_TD \tab integer \tab Passing touchdowns. \cr
   Int \tab integer \tab Interceptions thrown. \cr
   YPA \tab double \tab Yards per pass attempt. \cr
   EPA \tab double \tab  \cr
   EPA_per_Play \tab double \tab EPA per play on the passer's plays. \cr
   WPA \tab double \tab  \cr
   SR \tab double \tab Success rate on the passer's plays. \cr
   Sck \tab integer \tab Times the passer was sacked. \cr
   CompPct \tab double \tab Completion percentage from the advanced box score. \cr
   xCompPct \tab double \tab Expected completion percentage from the per-play completion model. \cr
   CPOE \tab double \tab Completion percentage over expected -- actual minus modelled completion rate. \cr
   qbr_epa \tab double \tab EPA variant used as an input to the QBR calculation. \cr
   sack_epa \tab double \tab EPA credited to the player's sacks taken. \cr
   pass_epa \tab double \tab EPA credited to the player's pass plays. \cr
   rush_epa \tab double \tab EPA credited to the player's rush plays. \cr
   pen_epa \tab double \tab EPA attributable to penalties on the player's plays. \cr
   spread \tab double \tab  \cr
   era0 \tab integer \tab Rule-era indicator for the earliest modelled era. \cr
   era1 \tab integer \tab Rule-era indicator for the second modelled era. \cr
   era2 \tab integer \tab Rule-era indicator for the third modelled era. \cr
   era3 \tab integer \tab Rule-era indicator for the most recent modelled era. \cr
   exp_qbr \tab double \tab Expected QBR for the passer. \cr
   game_id \tab integer \tab  \cr
   season \tab integer \tab  \cr
   week \tab integer \tab  \cr
}
}
\description{
Load espn_cfb_adv_passing (sportsdataverse-data release). Published to the
\code{espn_cfb_adv_passing} release tag on the sportsdataverse-data repo.
}
\examples{
\donttest{
  try(load_espn_cfb_adv_passing(2004))
}
}
\author{
Saiem Gilani
}
