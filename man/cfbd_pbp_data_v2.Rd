% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cfbd_pbp_data_v2.R
\name{cfbd_pbp_data_v2}
\alias{cfbd_pbp_data_v2}
\title{\strong{Get college football play-by-play data — modular EPA/WPA pipeline (v2)}}
\usage{
cfbd_pbp_data_v2(
  year,
  season_type = "regular",
  week = 1,
  team = NULL,
  play_type = NULL,
  epa_wpa = FALSE,
  output = "default"
)
}
\arguments{
\item{year}{(\emph{Numeric} required): Season year (e.g. \code{2024}). \cr
Minimum value accepted: 2001}

\item{season_type}{(\emph{Character}): Season type — \code{"regular"} (default),
\code{"postseason"}, \code{"both"}, \code{"allstar"}, \code{"spring_regular"},
\code{"spring_postseason"}.}

\item{week}{(\emph{Numeric}): Week number.}

\item{team}{(\emph{Character}): Optional team filter (e.g. \code{"Texas"}).}

\item{play_type}{(\emph{Character}): Optional play-type filter (see
\link{cfbd_play_type_df}).}

\item{epa_wpa}{(\emph{Logical}): When \code{TRUE}, run the EPA/WPA pipeline and
return the modeled frame; when \code{FALSE} (default) return the raw plays +
drives + betting join.}

\item{output}{(\emph{Character}): controls the modeled-output column set when
\code{epa_wpa = TRUE}. Ignored when \code{epa_wpa = FALSE}. Defaults to
\code{"default"}. Must be one of:
\itemize{
\item \code{"default"} (recommended) -- drops pipeline lag/lead
intermediates, redundant alternates (\code{sack_vec}, \code{turnover_indicator},
\code{kick_play}, \code{missing_yard_flag}), and drive-result aliases
(\code{drive_result2}, \code{drive_result_detailed_flag},
\code{lag_drive_result_detailed}, \code{lead_drive_result_detailed},
\code{lag_new_drive_pts}). Keeps \code{orig_play_type} and \code{pts_scored} (they
carry useful per-play information distinct from the canonical
columns) and the per-branch WPA scratchpad (\code{wpa_base}/\code{wpa_change}
etc.). ~75 columns lighter than \code{"full"} with no loss of information
that isn't trivially rebuildable.
\item \code{"lean"} -- everything \code{"default"} drops, plus the WPA
computation scratchpad. For dashboards / leaderboards / game logs.
\item \code{"full"} -- legacy behavior, drops only the player-name
aliases. For sequential modeling that consumes pre-computed lag/lead
shifts or the per-branch WPA decomposition.
}}
}
\value{
A \code{cfbfastR_data} tibble. The \code{epa_wpa = TRUE} output matches the
legacy \code{\link[=cfbd_pbp_data]{cfbd_pbp_data()}} pipeline-canonical column set on every column
it carries; the \code{output} argument controls which intermediate columns
are retained. Documented bug-fix sites are listed in the package
vignette.
}
\description{
Returns CFBD play-by-play data with optional Expected Points
Added (EPA) and Win Probability Added (WPA) modeling. The modular
successor to \code{\link[=cfbd_pbp_data]{cfbd_pbp_data()}}: a thin orchestrator over the shared
EPA/WPA engine (\code{.run_epa_wpa()}), the canonical play-type taxonomy
(\code{.pbp_play_types()}), and the canonical output schema
(\code{.pbp_output_order}). Side-by-side with the legacy entry point until the
equivalence harness proves the new path matches.
}
\examples{
\donttest{
  x <- try(cfbd_pbp_data_v2(
    year = 2024, week = 1, season_type = "regular",
    epa_wpa = TRUE, output = "default"
  ))
}
}
\seealso{
Other CFBD PBP: 
\code{\link{cfbd_live_plays}()},
\code{\link{cfbd_pbp_data}()},
\code{\link{cfbd_play_stats_player}()},
\code{\link{cfbd_play_stats_types}()},
\code{\link{cfbd_play_types}()},
\code{\link{cfbd_plays}()}
}
\concept{CFBD PBP}
\keyword{CFB}
\keyword{PBP}
