% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/espn_cfb_game.R
\name{espn_cfb_pbp_v2}
\alias{espn_cfb_pbp_v2}
\title{\strong{Get ESPN College Football Play-by-Play (core-v2)}}
\usage{
espn_cfb_pbp_v2(game_id, epa_wpa = FALSE)
}
\arguments{
\item{game_id}{(\emph{Integer} required): ESPN game identifier.}

\item{epa_wpa}{(\emph{Logical}): when \code{TRUE}, run the full EPA/WPA modeling
pipeline and return the modeled frame; when \code{FALSE} (default) return the
assembled core-v2 play-by-play frame.}
}
\value{
A data frame with one row per play. When \code{epa_wpa = FALSE}, the
assembled core-v2 play-by-play frame:\tabular{lll}{
   col_name \tab types \tab description \cr
   game_id \tab character \tab ESPN game identifier. \cr
   play_id \tab character \tab ESPN play id. \cr
   type_text \tab character \tab Play type label (e.g. \code{Rush}, \verb{Pass Reception}). \cr
}


(plus the full \code{\link[=espn_cfb_game_drives]{espn_cfb_game_drives()}} \code{plays = "expand"} column set --
play start/end down, distance, yard line, clock, scores, the \verb{drive_*}
drive context, team detail -- and the attached game-metadata columns
\code{season}, \code{season_type}, \code{week}, \code{neutral_site},
\code{conference_competition}, \code{game_date}, \code{home_team_id}, \code{home_team},
\code{away_team_id}, \code{away_team}).

When \code{epa_wpa = TRUE}, every column from the \code{epa_wpa = FALSE} frame
above (game metadata, \verb{drive_*} context, team detail and the full
\code{\link[=espn_cfb_game_drives]{espn_cfb_game_drives()}} play schema) \strong{plus} the modeled EPA/WPA
columns produced by the existing pipeline -- \code{ep_before}, \code{ep_after},
\code{EPA}, \code{wp_before}, \code{wp_after}, \code{wpa} and the supporting probability and
cumulative columns. The \code{epa_wpa = TRUE} columns are a strict superset
of the \code{epa_wpa = FALSE} columns, and the row count is unchanged.
}
\description{
The core-v2-sourced successor to \code{\link[=espn_cfb_pbp]{espn_cfb_pbp()}}. Returns
one tidy row per play for a single college football game, optionally with
the full Expected Points Added (EPA) / Win Probability Added (WPA)
modeling attached. It supersedes the legacy site-v2 \code{\link[=espn_cfb_pbp]{espn_cfb_pbp()}}:
instead of parsing the deeply-nested site-v2 \code{summary} feed, it sources
the play-by-play from the structured ESPN core-v2 drives endpoint in a
single request via \code{\link[=espn_cfb_game_drives]{espn_cfb_game_drives()}} -- one structured call rather
than a sprawling summary parse, so it is faster and more robust to the
site-v2 feed's column drift.
}
\details{
\strong{Play-by-play assembly (always).} The play rows come from
\code{espn_cfb_game_drives(game_id, plays = "expand", team_detail = TRUE)} --
one row per play, in the full \code{\link[=espn_cfb_game_pbp]{espn_cfb_game_pbp()}} schema, with each
play's drive-level context carried alongside (\verb{drive_*} columns) and the
ESPN team catalog joined onto every team-id column. Game-level metadata
matching the legacy \code{\link[=espn_cfb_pbp]{espn_cfb_pbp()}} output -- \code{season}, \code{season_type},
\code{week}, \code{neutral_site}, \code{conference_competition}, \code{game_date}, and the
\code{home_team_id} / \code{home_team} / \code{away_team_id} / \code{away_team} (plus
abbreviations) -- is attached from the core-v2 event resource and
\code{\link[=espn_cfb_game_teams]{espn_cfb_game_teams()}}.

\strong{Game-meta bridge.} Season / season_type / week / neutral_site /
conference_competition / game_date and the full home/away block --
including \verb{*_team_name}, \verb{*_team_color}, \verb{*_team_alternate_color}, and
the week-specific \verb{*_team_rank} -- are sourced via
\code{.espn_pbp_game_meta()}, which queries the core-v2 event resource. This
makes \code{espn_cfb_pbp_v2()} a strict meta-column superset of legacy
\code{\link[=espn_cfb_pbp]{espn_cfb_pbp()}}.

\strong{Native participant columns.} This wrapper requests
\code{participants = "wide"} and \code{team_participants = "wide"} from
\code{\link[=espn_cfb_game_drives]{espn_cfb_game_drives()}}, so the play frame carries type-keyed
\verb{\{type\}_player_id} / \verb{\{type\}_player_name} / \verb{\{type\}_player_position}
columns (e.g. \code{passer_player_id}, \code{rusher_player_id}, \code{tackler_player_id})
and \verb{\{type\}_team_*} columns. Wide mode also emits the
\verb{\{type\}_player_ids} / \verb{\{type\}_player_names} list-columns; downstream
parquet/CSV writers must drop or stringify these before serialization.
One additional roster fetch + position-catalog fetch per game.

\strong{\code{epa_wpa} behaviour.} When \code{epa_wpa = FALSE} (default) the assembled
core-v2 frame is returned as-is. When \code{epa_wpa = TRUE} the core-v2
columns are mapped into the exact column schema the legacy
\code{\link[=espn_cfb_pbp]{espn_cfb_pbp()}} hands to its EPA/WPA pipeline, and the \strong{same} modeling
stack is run verbatim -- \code{penalty_detection()}, \code{add_play_counts()},
\code{clean_pbp_dat()}, \code{clean_drive_dat()}, \code{add_yardage()},
\code{add_player_cols()}, \code{prep_epa_df_after()}, \code{create_epa()} and
\code{create_wpa_naive()}. The statistical models (the \code{mgcv} GAMs, the
multinomial EP model, the FG model and the WP model shipped with the
package) are reused unchanged; this wrapper only supplies the column
adapter between the core-v2 play schema and the modeling input contract.
The modeled EPA/WPA columns the pipeline emits -- \code{ep_before},
\code{ep_after}, \code{EPA}, \code{wp_before}, \code{wp_after}, \code{wpa}, and the supporting
probability and cumulative columns -- are then joined back onto the
\strong{full} \code{epa_wpa = FALSE} context frame by \code{play_id}. The
\code{epa_wpa = TRUE} output is therefore a strict \strong{superset} of the
\code{epa_wpa = FALSE} output: every game-metadata and drive-context column
the default path produces, plus the EPA/WPA model columns. The join is a
left join from the context frame, so no play row is dropped and the row
count is identical to the \code{epa_wpa = FALSE} result.
}
\examples{
\donttest{
  try(espn_cfb_pbp_v2(game_id = 401628339, epa_wpa = TRUE))
}
}
\seealso{
Other ESPN CFB Functions: 
\code{\link{espn_cfb_award}()},
\code{\link{espn_cfb_awards}()},
\code{\link{espn_cfb_clear_cache}()},
\code{\link{espn_cfb_coach}()},
\code{\link{espn_cfb_coach_record}()},
\code{\link{espn_cfb_coaches}()},
\code{\link{espn_cfb_franchise}()},
\code{\link{espn_cfb_franchises}()},
\code{\link{espn_cfb_futures}()},
\code{\link{espn_cfb_game_broadcasts}()},
\code{\link{espn_cfb_game_drive_plays}()},
\code{\link{espn_cfb_game_drives}()},
\code{\link{espn_cfb_game_leaders}()},
\code{\link{espn_cfb_game_odds}()},
\code{\link{espn_cfb_game_pbp}()},
\code{\link{espn_cfb_game_play}()},
\code{\link{espn_cfb_game_player_box}()},
\code{\link{espn_cfb_game_player_statistics}()},
\code{\link{espn_cfb_game_powerindex}()},
\code{\link{espn_cfb_game_predictor}()},
\code{\link{espn_cfb_game_probabilities}()},
\code{\link{espn_cfb_game_situation}()},
\code{\link{espn_cfb_game_status}()},
\code{\link{espn_cfb_game_team_leaders}()},
\code{\link{espn_cfb_game_team_linescores}()},
\code{\link{espn_cfb_game_team_records}()},
\code{\link{espn_cfb_game_team_roster}()},
\code{\link{espn_cfb_game_team_statistics}()},
\code{\link{espn_cfb_game_teams}()},
\code{\link{espn_cfb_groups}()},
\code{\link{espn_cfb_player}()},
\code{\link{espn_cfb_player_eventlog}()},
\code{\link{espn_cfb_player_gamelog}()},
\code{\link{espn_cfb_player_overview}()},
\code{\link{espn_cfb_player_seasons}()},
\code{\link{espn_cfb_player_splits}()},
\code{\link{espn_cfb_player_statistics}()},
\code{\link{espn_cfb_players}()},
\code{\link{espn_cfb_position}()},
\code{\link{espn_cfb_positions}()},
\code{\link{espn_cfb_powerindex}()},
\code{\link{espn_cfb_qbr}()},
\code{\link{espn_cfb_rankings}()},
\code{\link{espn_cfb_recruits}()},
\code{\link{espn_cfb_season_info}()},
\code{\link{espn_cfb_season_types}()},
\code{\link{espn_cfb_season_weeks}()},
\code{\link{espn_cfb_seasons}()},
\code{\link{espn_cfb_standings}()},
\code{\link{espn_cfb_team}()},
\code{\link{espn_cfb_team_ats}()},
\code{\link{espn_cfb_team_awards}()},
\code{\link{espn_cfb_team_coaches}()},
\code{\link{espn_cfb_team_events}()},
\code{\link{espn_cfb_team_leaders}()},
\code{\link{espn_cfb_team_powerindex}()},
\code{\link{espn_cfb_team_ranks}()},
\code{\link{espn_cfb_team_record}()},
\code{\link{espn_cfb_team_roster}()},
\code{\link{espn_cfb_team_schedule}()},
\code{\link{espn_cfb_teams}()},
\code{\link{espn_cfb_unnest_plays}()},
\code{\link{espn_cfb_venue}()},
\code{\link{espn_cfb_venues}()},
\code{\link{espn_cfb_week_rankings}()}
}
\concept{ESPN CFB Functions}
\keyword{CFB}
\keyword{PBP}
