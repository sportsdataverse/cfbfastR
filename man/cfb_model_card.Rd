% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_cards.R
\name{cfb_model_card}
\alias{cfb_model_card}
\alias{cfb_card_features}
\alias{cfb_card_era_contract}
\alias{cfb_card_reset_cache}
\title{\strong{Read the published contract for a CFB model}}
\usage{
cfb_model_card(model)

cfb_card_features(model)

cfb_card_era_contract(model)

cfb_card_reset_cache()
}
\arguments{
\item{model}{(\emph{String} required): Bundle stem, e.g. \code{"ep_model"} or \code{"wp_spread"}.}
}
\value{
\code{\link[=cfb_model_card]{cfb_model_card()}} - A list with the card's fields, including:\tabular{lll}{
   col_name \tab types \tab description \cr
   model_type \tab character \tab Model family, e.g. "ep", "fg", "xpass". \cr
   label \tab character \tab Training label the model was fit against. \cr
   features \tab character \tab Ordered feature names the booster was trained with. \cr
   era_contract \tab list \tab Era encoding and cutpoints, or \code{NULL} when the model uses no era. \cr
}

\code{\link[=cfb_card_features]{cfb_card_features()}} - A character vector of feature names in the
order the booster was trained with. Order is load-bearing: xgboost aligns a
DMatrix by position, so a sorted or re-derived list scores against the
wrong columns without ever erroring.

\code{\link[=cfb_card_era_contract]{cfb_card_era_contract()}} - A list with \code{encoding} (\code{"ordinal"} or
\code{"one_hot"}), \code{columns} and \code{cuts}, or \code{NULL} when the model consumes no
era feature. \code{NULL} is correct for \code{ep_model}, \code{wp_naive}, \code{wp_spread} and
\code{cfb_cp_model}.

\code{\link[=cfb_card_reset_cache]{cfb_card_reset_cache()}} - Invisibly \code{NULL}, called for its effect.
}
\description{
Each model in the \code{cfb_model_artifacts} bundle ships a \verb{<model>.card.json}
carrying the ordered \code{features} array it was trained with and, where the
model consumes one, an \code{era_contract}. Reading that contract lets a caller's
frame be validated against the artifact itself rather than a feature list
restated in this package -- the duplication that produced
cfbfastR-cfb-data#70, where this package kept a private 2017 era cut the
trainer never used and 2018-2020 scored an era off the models trained with
them.
\itemize{
\item \code{cfb_model_card()}: the parsed card.
\item \code{cfb_card_features()}: the model's feature names, in trained order.
\item \code{cfb_card_era_contract()}: the era encoding, or \code{NULL}.
\item \code{cfb_card_reset_cache()}: drop the in-session card cache.
}
}
\examples{
\donttest{
  try(cfb_card_features("ep_model"))
}
}
\concept{CFB Model Calculators}
\keyword{CFB}
\keyword{Card}
\keyword{Model}
