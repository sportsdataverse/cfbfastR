% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cfbd_pbp_data_v2.R
\name{cfbd_pbp_v2}
\alias{cfbd_pbp_v2}
\alias{pbp_v2}
\alias{modular_epa}
\alias{modular_wpa}
\title{\strong{CFBD Play-by-Play (v2 Modular EPA/WPA Pipeline) Overview}}
\description{
\itemize{
\item \code{cfbd_pbp_data_v2()}: Get college football play-by-play data — modular
EPA/WPA pipeline (v2). Thin orchestrator over the shared EPA/WPA engine
\code{.run_epa_wpa()}, the canonical play-type taxonomy \code{.pbp_play_types()},
and the canonical output schema \code{.pbp_output_order}. Runs side-by-side
with the legacy \code{\link[=cfbd_pbp_data]{cfbd_pbp_data()}} entry point until the equivalence
harness proves the new path matches.
}
}
\details{
The v2 entry point is a thin wrapper around \code{.run_epa_wpa()} -- the
shared engine that powers both the modular (v2) and legacy paths. The
\code{output = "default" / "lean" / "full"} tier argument selects which
intermediate columns survive the final select: \code{"default"} drops
pipeline lag/lead intermediates and redundant alternates, \code{"lean"}
additionally drops the per-branch WPA scratchpad, and \code{"full"} is the
legacy column set (drops only player-name aliases). The
equivalence-harness allow-list is intentionally permissive about
lag/lead intermediates and per-branch WPA scratchpad columns because
those are mechanically rebuildable from the surviving canonical
columns; the harness only enforces equality on user-facing values.
\subsection{\strong{Get college football play-by-play data — modular EPA/WPA pipeline (v2)}}{

\if{html}{\out{<div class="sourceCode r">}}\preformatted{cfbd_pbp_data_v2(
  year = 2024, week = 1, season_type = "regular",
  epa_wpa = TRUE, output = "default"
)
}\if{html}{\out{</div>}}
}
}
