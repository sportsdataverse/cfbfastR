% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_espn_cfb.R
\name{load_espn_cfb_adv_receiving}
\alias{load_espn_cfb_adv_receiving}
\title{\strong{Load college football advanced receiving from the SportsDataverse data repo}}
\usage{
load_espn_cfb_adv_receiving(
  seasons = most_recent_cfb_season(),
  ...,
  dbConnection = NULL,
  tablename = NULL
)
}
\arguments{
\item{seasons}{A vector of 4-digit years associated with given college football seasons. Published coverage runs 2004 through the most recent season. Pass \code{seasons = TRUE} for every published season. (Min: 2004)}

\item{...}{Additional arguments passed to an underlying function that
writes the season data into a database.}

\item{dbConnection}{A \code{DBIConnection} object, as returned by \code{\link[DBI:dbConnect]{DBI::dbConnect()}}}

\item{tablename}{The name of the data table within the database}
}
\value{
Returns a \code{cfbfastR_data} tibble.\tabular{lll}{
   col_name \tab types \tab description \cr
   pos_team_id \tab integer \tab ESPN team id of the team on offense. Present for every season 2004+. \cr
   pos_team \tab character \tab  \cr
   receiver_player_name \tab character \tab Display name of the targeted receiver -- the FIRST participant in that role on the play. \cr
   Rec \tab integer \tab Receptions credited to the receiver, the number of completions on plays where this player was the targeted receiver. \cr
   Tar \tab integer \tab Times the player was targeted on a pass attempt, the denominator behind YPT. \cr
   Yds \tab double \tab Passing yards from the advanced box score. \cr
   Rec_TD \tab integer \tab Receiving touchdowns, the count of the player's targeted plays that ended in a passing touchdown. \cr
   YPT \tab double \tab Receiving yards per target, the mean of receiving yardage over every target rather than over receptions only. \cr
   EPA \tab double \tab  \cr
   EPA_per_Play \tab double \tab EPA per play on the passer's plays. \cr
   WPA \tab double \tab  \cr
   SR \tab double \tab Success rate on the passer's plays. \cr
   Fum \tab integer \tab Count of the receiver's targeted pass plays whose text mentions a fumble; it is a play-level flag, not a fumble charged to this player. \cr
   Fum_Lost \tab integer \tab Count of the receiver's targeted plays on which a fumble was lost to the opponent. \cr
   game_id \tab integer \tab  \cr
   season \tab integer \tab  \cr
   week \tab integer \tab  \cr
}
}
\description{
Loads season-level advanced receiving stats -- one row per qualifying
receiver with target share, EPA per target, and YAC splits. Published
to
the \code{espn_cfb_adv_receiving} release tag on the sportsdataverse-data repo.
}
\examples{
\donttest{
  try(load_espn_cfb_adv_receiving(2004))
}
}
\author{
Saiem Gilani
}
