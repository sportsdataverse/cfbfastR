% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_espn_cfb.R
\name{load_espn_cfb_adv_specialists}
\alias{load_espn_cfb_adv_specialists}
\title{\strong{Load college football advanced specialists from the SportsDataverse data repo}}
\usage{
load_espn_cfb_adv_specialists(
  seasons = most_recent_cfb_season(),
  ...,
  dbConnection = NULL,
  tablename = NULL
)
}
\arguments{
\item{seasons}{A vector of 4-digit years associated with given college football seasons. Published coverage runs 2004 through the most recent season. Pass \code{seasons = TRUE} for every published season. (Min: 2004)}

\item{...}{Additional arguments passed to an underlying function that
writes the season data into a database.}

\item{dbConnection}{A \code{DBIConnection} object, as returned by \code{\link[DBI:dbConnect]{DBI::dbConnect()}}}

\item{tablename}{The name of the data table within the database}
}
\value{
Returns a \code{cfbfastR_data} tibble.\tabular{lll}{
   col_name \tab types \tab description \cr
   pos_team_id \tab integer \tab ESPN team id of the team on offense. Present for every season 2004+. \cr
   pos_team \tab character \tab  \cr
   player_name \tab character \tab  \cr
   field_goals \tab integer \tab Number of field-goal attempts. \cr
   field_goals_yards \tab integer \tab Sum of the field-goal attempt distances parsed out of the play text; it stays at zero when no distance could be parsed from the narrative. \cr
   punts \tab integer \tab Punts attempted. \cr
   punts_yards \tab integer \tab Total gross punt yardage parsed from the play text for this punter, working out to roughly 42 yards per punt league-wide. \cr
   kick_returns \tab integer \tab  \cr
   kick_returns_yards \tab integer \tab Total yards the team gained returning kickoffs. \cr
   punt_returns \tab integer \tab  \cr
   punt_returns_yards \tab integer \tab Total punt-return yardage credited to this returner, with fair catches, downed punts, and out-of-bounds punts scored as zero. \cr
   game_id \tab integer \tab  \cr
   season \tab integer \tab  \cr
   week \tab integer \tab  \cr
}
}
\description{
Loads season-level specialist stats -- one row per
kicker/punter/returner with kicking EPA, field-goal profile, and return
aggregates. Published to
the \code{espn_cfb_adv_specialists} release tag on the sportsdataverse-data
repo.
}
\examples{
\donttest{
  try(load_espn_cfb_adv_specialists(2004))
}
}
\author{
Saiem Gilani
}
